apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-scale-25
  labels:
    appname: scale-partition-dr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sc-scale-2-partition-dr
  resources:
    requests:
      storage: 40Gi
