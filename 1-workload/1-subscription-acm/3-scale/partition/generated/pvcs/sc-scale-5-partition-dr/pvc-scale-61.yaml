apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-scale-61
  labels:
    appname: scale-partition-dr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sc-scale-5-partition-dr
  resources:
    requests:
      storage: 40Gi
