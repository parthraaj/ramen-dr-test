apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-scale-62
  labels:
    appname: scale-non-partition-dr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sc-scale-5-non-partition-dr
  resources:
    requests:
      storage: 40Gi
