apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-scale-13
  labels:
    appname: scale-non-partition-dr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sc-scale-1-non-partition-dr
  resources:
    requests:
      storage: 40Gi
