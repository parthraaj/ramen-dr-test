apiVersion: ramendr.openshift.io/v1alpha1
kind: DRPlacementControl
metadata:
  name: stress-partition-dr-drpc
  namespace: stress-partition-dr
spec:
  preferredCluster: ramen-ocp1
  drPolicyRef:
    name: ramen-policy-1-2-stress-partition
  placementRef:
    kind: Placement
    name: stress-partition-dr-app-placement-1
  pvcSelector:
    matchLabels:
      appname: stress-partition-dr
