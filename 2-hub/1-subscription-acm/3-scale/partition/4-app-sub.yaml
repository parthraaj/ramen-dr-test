apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: scale-partition-dr-app
  namespace: scale-partition-dr
spec:
  componentKinds:
    - group: apps.open-cluster-management.io
      kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - scale-partition-dr-app

---
apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  annotations:
    apps.open-cluster-management.io/git-branch: main
    apps.open-cluster-management.io/git-path: 1-workload/3-scale/partition
    apps.open-cluster-management.io/reconcile-option: merge
  labels:
    app: scale-partition-dr-app
  name: scale-partition-dr-sub-1
  namespace: scale-partition-dr
spec:
  channel: ggithubcom-parthraaj-ramen-dr-test-ns/ggithubcom-parthraaj-ramen-dr-test
  placement:
    placementRef:
      kind: Placement
      name: scale-partition-dr-app-placement-1
