apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argocd-basic-rw-partition-appset
  namespace: openshift-gitops
spec:
  generators:
    - clusterDecisionResource:
        configMapRef: argocd-basic-rw-partition-appset
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: argocd-basic-rw-partition-appset
        requeueAfterSeconds: 30
  template:
    metadata:
      name: 'argocd-basic-rw-partition-{{name}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/parthraaj/ramen-dr-test
        targetRevision: main
        path: 3-workload/2-argocd/1-basic-rw/partition
      destination:
        server: '{{server}}'
        namespace: argocd-basic-rw-partition-dr
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - PruneLast=true
